import { Command } from "../commands.ts";
/**
 * The `echo` command. Outputs the provided arguments to stdout.
 *
 * If no arguments are provided, it will output an empty string.
 * @type {Command}
 */
declare const echo: Command;
export { echo };
